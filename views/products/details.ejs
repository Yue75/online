<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails du produit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center text-primary">Détails du produit</h1>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title">Nom : <%= product.nom %></h4>
                <p class="card-text"><strong>Description :</strong> <%= product.description %></p>
                <p class="card-text"><strong>Prix :</strong> <%= product.prix %> €</p>
                <p class="card-text"><strong>Stock :</strong> <%= product.stock %></p>
                <p class="card-text"><strong>Code EAN :</strong> <%= product.codeEAN %></p>
                <p class="card-text"><strong>Catégorie :</strong> <%= product.categorieNom || "Non spécifiée" %></p>
            </div>
        </div>
        <div class="mt-3 text-center">
            <a href="/produits" class="btn btn-secondary">Retour à la liste des produits</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
